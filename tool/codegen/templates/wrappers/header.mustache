// Copyright (c) 2016, Herman Bergwerf. All rights reserved.
// Use of this source code is governed by a MIT-style license
// that can be found in the LICENSE file.

#ifndef {{{ header }}}
#define {{{ header }}}

#include <FL/Fl.H>
#include <FL/{{{ class }}}.H>

#include "dart_api.h"
#include "../../common.h"

namespace fldart {
class {{{ class }}}_Wrapper : public {{{ class }}} {
  Dart_PersistentHandle _ref;

 public:
  {{# constructors }}
  {{! Fails if argsdef is empty, but just go with it for now. }}
  {{{ class }}}_Wrapper(Dart_Handle ref, {{{ argsdef }}});
  {{/ constructors }}

  {{# drawcb }}
  void draw();
  {{/ drawcb }}
  static void callback_wrapper(Fl_Widget*, void*);
};
}

#endif
